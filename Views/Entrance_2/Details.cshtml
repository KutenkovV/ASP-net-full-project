@model Accounting_of_Goods_ver_2.Models.Entrance_2

@{
    ViewBag.Title = "Детали";
}

<h2>Детали</h2>

<div>
    <h4>Второе поступление</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Product_quantity)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Product_quantity)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Product.name_of_the_company)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Product.name_of_the_company)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Retail_outlets.name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Retail_outlets.name)
        </dd>

    </dl>
</div>
<p>
    <a class="btn btn-outline-secondary" href="@Url.Action("Edit", new { id = Model.ID })">Редактировать</a>
    <a class="btn btn-outline-secondary" href="@Url.Action("Index")">Вернуться к таблице</a>
</p>
